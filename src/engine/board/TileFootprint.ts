export interface TileFootprint {
  width: number;    // x axis (topleft-to-bottomright in isometric projection)
  height: number;   // y axis (topright-to-bottomleft in isometric projection)
  depth: number;    // z axis (vertically upwards in an isometric projection)
}
